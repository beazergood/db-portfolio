{
  "name": "db-portfolio",
  "version": "2.0.0",
  "private": true,
  "description": "Personal portfolio + living wiki for Dave Beazer.",
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:site": "cd static && python3 -m http.server 8080",
    "dev:docs": "cd docs && python3 -m http.server 8081",
    "stop": "lsof -ti:8080,8081 | xargs -r kill 2>/dev/null; echo 'stopped'"
  }
}
